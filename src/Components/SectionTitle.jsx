

const SectionTitle = ({title}) => {
    return (
        <div>
            <h2 className="text-center my-4 p-4 text-3xl font-bold text-alpha">{title}</h2>
        </div>
    );
};

export default SectionTitle;